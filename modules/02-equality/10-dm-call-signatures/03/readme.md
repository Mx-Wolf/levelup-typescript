**Реализация диспетчера событий**

При реализации диспетчера воспользуемся тем, что функции в JavaScript (и в TypeScript) являются в том числе обычными значениями. `Object.assign` поможет нам в этом.
